# 0.3.1 — 2026-09-23

本轮复查修复：

- 项目目录、界面名称及 EXE 统一为 `WinSw_winPackaging`，与 GitHub 仓库同名。

- 修复日志文件前缀判断，`app` 不再读取或清空 `app.worker` 的日志。
- 清屏直接跳过所有已存在的磁盘内容，大量积压日志不再在清屏后重新出现。
- 更新前校验 JAR 目标路径和 XML，源文件与部署文件相同或 XML 参数无效时不再先停止服务。
- 删除目录中途遇到文件锁时保留 XML 清理标记，服务仍出现在列表中，可以再次清理。
- 所有配置字段一次保存；写入失败回滚内存，避免部分生效；应用目录留空可恢复默认位置。
- 应用目录创建失败不更改管理器当前目录；列表读取异常不再使上传页永久屏蔽选择信号。
- 添加 Git 忽略规则、开发依赖、Windows 自动测试与单 EXE 构建流程，保留仓库原有 Apache-2.0 许可证。

验证：50 项本地回归测试，Windows 单 EXE 构建和管理员权限声明检查。测试使用临时文件及模拟服务 API，未对真实业务服务执行安装、停止或删除。

# 0.3.0 修复说明

## 单文件发行

- 将管理员权限声明直接嵌入主程序，PyInstaller 只生成一个 `WinSW_Manager.exe`，不再依赖独立启动器。

## 服务生命周期

- 删除服务不再执行全局 `taskkill /IM java.exe`。先停止目标服务，等待 Windows 确认停止，再卸载；确认服务不存在后才清理其目录。
- 服务名、根目录边界、XML ID 和 Windows 注册的可执行文件路径都经过检查，防止误操作同名外部服务。
- 使用 Windows 原生数字状态，避免 `sc.exe` 中英文输出差异；正确区分启动中、停止中、暂停中和恢复中。
- 启动、停止、重启等待真实状态；停止超时、权限不足、启动失败均返回失败，不继续执行删除或覆盖 JAR。
- 后台执行耗时操作，Qt 事件循环保持响应；操作期间禁止关闭窗口或重复提交。
- 默认两秒刷新服务状态；关闭普通刷新后，仍跟踪过渡状态。表格不可编辑，刷新按服务ID保持选择，删除后不会误选另一行。

## 日志控制台

- 三个页面使用同一套增量日志读取组件，默认显示 stdout 和 stderr，完整保留异常堆栈；WinSW 自身诊断独立显示。
- 支持现有 XML 的 `logpath`、`%BASE%`、`logname`，并兼容服务根目录和 `logs/` 下的旧日志。
- 支持实时追加、轮转、截断检测、UTF-8 分段解码、GB18030 编码切换、暂停、清屏、查找和回到最新。
- 默认保留最近 2,000 行，可提高到 100,000 行并重新加载磁盘历史；完整磁盘日志通过“打开日志目录”访问。
- 向上滚动时保持阅读位置，不再每次刷新强制跳到底部；重复刷新不重复添加内容。
- “清屏”不删除日志文件；“清空磁盘日志”保留文件本身，文件被占用等错误会明确报告。
- 修正日志滚动阈值单位：WinSW 的 `sizeThreshold` 是 KB，100 MB 应为 102400，而不是 104857600。

注意：WinSW 已把 stdout 和 stderr 写入两个文件，无法恢复两个流原始的精确交错顺序。历史按文件修改顺序加载，实时输出按读取顺序追加。应用日志如果只写自定义业务文件、没有写控制台，需要配置应用同时向 stdout/stderr 输出。滚动策略已经删除的历史无法恢复。

## 更新与配置

- 更新前停止失败即终止该任务；未停止的服务拒绝覆盖 JAR；只有更新成功才执行启动。
- JAR 和 XML 先写临时文件再替换；已有同名 JAR 和原 XML 备份为 `.bak`，XML 写入失败会回滚 JAR。
- 不再以当前上传目录拼接历史任务文件名，每个任务保存绝对源路径；防止不同目录的同名 JAR 混用。
- 修复更新任务删除行后按钮索引错位，禁止重复添加同一服务任务。
- 更新 XML 只修改日志相关设置，保留原有 JDK、JVM 参数、Spring 参数、环境变量和自定义节点。
- 新服务 XML 正确转义特殊字符、引用带空格的 JAR 文件名，检测无效 JDK 和重复服务ID。
- 修改应用目录时所有页面共享同一个管理器，并清除旧目录的待更新任务。
- 配置文件采用原子写入；无效 JSON 结构可恢复。
- WinSW 下载使用 GitHub 实际 release asset，不再拼接不存在的 ZIP；下载失败不伪造版本，临时下载不会被当作完整程序。

## 验证范围

`python -m unittest discover -s tests -v`

回归测试使用临时部署目录、模拟 Windows 服务 API 和离屏 Qt 窗口，不会启停、删除真实业务服务。覆盖五服务删除隔离、权限与超时失败、启动失败、配置保留、JAR 回滚、日志轮转/截断/中文追加、跨页面刷新和后台线程响应。

Windows 原生状态 API 已通过只读系统服务查询验证。尚未对真实业务 JAR 做 Windows 服务安装/卸载的端到端测试。Windows `Running` 表示服务已启动，不代表 Spring Boot 的端口或业务健康检查已经就绪；以应用启动日志为准。

参考：[WinSW 日志说明](https://github.com/winsw/winsw/blob/v3/docs/logging-and-error-reporting.md)、[Windows 服务状态转换](https://learn.microsoft.com/en-us/windows/win32/services/service-status-transitions)。
